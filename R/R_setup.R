
pkgs <- c("igraph",
          "igraphdata",
           "influenceR",
           "intergraph",
           "magrittr",
           "network",
           "remotes",
           "sna")

install.packages(pkgs = pkgs, dependencies = TRUE)
